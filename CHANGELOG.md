# Changelog — Especificação do Manual Mestre

Formato: [Keep a Changelog](https://keepachangelog.com/) simplificado. Uma entrada por alteração da especificação.

## [3.0.0] — 2026-07-30

### Adicionado
- Especificação de arquitetura completa (`manualMestre_v3.0.md`), substituindo integralmente a v2.0.
- Início da geração: estrutura do repositório, arquivos de raiz, `00-visao-do-modulo.md` do módulo 00 e capítulo 00.01.
- Módulo 00 completo (capítulos 00.02–00.05, exercícios e gabaritos, guias de ambiente, pacote de revisão, simulados CP2 variantes A/B, cheatsheet de método). Decisões D-004 a D-006 registradas.
- Módulo 01 iniciado: visão do módulo (objetivos dos 25 capítulos) + capítulos 01.01–01.04 com código executável, exercícios, gabaritos, flashcards e banco de entrevistas.
- Capítulos 01.05–01.08 (strings 1 e 2, entrada/saída, booleanos): código executável testado, exercícios e gabaritos, 40 flashcards e 25 perguntas de entrevista acumuladas no módulo.
- Capítulos 01.09–01.12 (condicionais, while, for/range, listas 1): promessas didáticas pagas (troco e tabela refatorados), 60 flashcards e 40 perguntas acumuladas.
- Capítulos 01.13–01.16 (aliasing/cópias, tuplas, dicionários, conjuntos): quarteto de estruturas completo; a dor original da Aurora ("quanto vendemos por cidade") respondida em 01.15. 80 flashcards e 55 perguntas acumuladas.
- Capítulos 01.17–01.20 (compreensões, funções 1 e 2, módulos/imports): a caixa-preta mais antiga da trilha (`if __name__ == "__main__"`, prometida em 01.02) aberta; biblioteca + dois programas com zero duplicação. 100 flashcards e 70 perguntas acumuladas.
- **Módulo 01 completo**: capítulos 01.21–01.25 (exceções, arquivos/CSV, JSON, depuração, PEP 8 + mini projeto) e pacote de fechamento — `revisao/` (resumo, mapa mental, 15 questões), `Simulados/modulo-01` A e B, cheatsheet de Python e 5 desafios de entrevista. Entrega Atlas: Relatório de Vendas Aurora v0. 125 flashcards no módulo.
- Módulo 02 iniciado: visão do módulo (objetivos dos 12 capítulos) + capítulos 02.01–02.04 (terminal, navegação/manipulação, inspeção, pipes/grep/find) com scripts de caderno executáveis, exercícios, gabaritos, 20 flashcards e 13 perguntas de entrevista. Decisão D-009 registrada (bash/Git Bash como shell de referência).
- Capítulos 02.05–02.08 (permissões/processos, variáveis de ambiente e PATH, scripts de shell, Git como modelo mental): a caixa-preta do PATH aberta desde o 00.03 e a da pasta `.git` vista no 02.01 fechadas; padrão de configuração por ambiente e `.env` fora do Git antecipado para o módulo 06. Scripts executados e conferidos no sandbox, incluindo o `verificar_manual.sh`, que audita o próprio repositório e devolve código de saída. 40 flashcards e 29 perguntas de entrevista acumuladas no módulo. Decisão D-010 registrada.
- **Módulo 02 completo**: capítulos 02.09–02.12 (fluxo essencial do Git com `.gitignore`, branches e merge, remotos/GitHub, desfazendo) e pacote de fechamento — `revisao/` (resumo, mapa mental, 15 questões), `Simulados/modulo-02` A e B, cheatsheet de Git/Linux e 4 desafios de entrevista. Entrega Atlas: repositório publicado no GitHub com fluxo de branches e histórico legível. 60 flashcards e 45 perguntas de entrevista no módulo. Decisões D-011 e D-012 registradas.
- Módulo 03 iniciado: visão do módulo (objetivos dos 16 capítulos, fio condutor em três movimentos) + capítulo 03.01 (por que bancos relacionais existem). Laboratório SQLite criado — `codigo/cap01/criar_laboratorio.py` gera o banco da Aurora com 4 tabelas e 71 linhas, e `codigo/sql.py` executa consultas de arquivo, argumento ou modo interativo. Casos de ensino embutidos nos dados (cliente sem compras, e-mail NULL, produto nunca vendido, pedido cancelado). Decisões D-013 e D-014 registradas.
- Capítulo 03.02 (tabelas, linhas e chaves): chave primária artificial, chave estrangeira e integridade referencial demonstradas com as **quatro recusas reais** do banco (FK inexistente, PK duplicada, NOT NULL, DELETE com dependentes), cada mensagem de erro verificada. Leitura de schema com `sqlite_master` e `pragma_table_info` como habilidade prática. 10 flashcards e 8 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.03 (`SELECT` e `WHERE`): operadores de filtro, `LIKE` com nota de dialeto (o SQLite ignora maiúsculas em ASCII e nunca ignora acento), precedência de `AND`/`OR` demonstrada com resultados divergentes (3 linhas × 1 linha), e a **lógica de três valores** do `NULL` — com as duas armadilhas silenciosas reproduzidas: `= NULL` devolvendo zero linhas e a negação que exclui os nulos (3 + 4 ≠ 8). 15 flashcards e 12 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.04 (ordenação, `LIMIT` e `DISTINCT`): `ORDER BY` com desempate, posição do `NULL` na ordenação (com a forma portável), `AS` e a ordem de execução que explica por que o apelido vale no `ORDER BY` e não no `WHERE`. O eixo do capítulo é o **bug de paginação sem ordenação total**, tratado até a paginação por cursor — com o reconhecimento honesto de que a versão frágil não quebra no laboratório estático, e é por isso que ele sobrevive em produção. 20 flashcards e 16 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.05 (funções de agregação): as cinco funções e a **regra única** — todas ignoram `NULL`, exceto `COUNT(*)`. Dela derivam as três consequências demonstradas com números reais: contagens que discordam (8/7/7/3 na mesma tabela), a média que muda de denominador (7,43 × 6,50) e o `SUM` de conjunto vazio devolvendo `NULL` em vez de zero. Antecipa a multiplicação de linhas da junção (28 × 17) e a prova dos nove feita **em centavos inteiros**, porque em reais o ponto flutuante não fecha. 25 flashcards e 20 perguntas acumuladas no módulo.
- Capítulo 03.06 (`GROUP BY` e `HAVING`): fecha o arco mais antigo da trilha — o `chave → acumulador` do 01.15 e o `sort | uniq -c` do 02.04 viram uma linha declarativa. A distinção `WHERE` (linhas, antes) × `HAVING` (grupos, depois) é demonstrada com a mesma tabela e o mesmo limiar produzindo resultados diferentes (audio com 3 e com 4). Registra que o `NULL` **forma seu próprio grupo** (invertendo a intuição do `WHERE`) e o terceiro caso de permissividade perigosa do SQLite: coluna fora do `GROUP BY` devolve valor arbitrário sem erro. Entrega a dor da Aurora respondida — faturamento por cidade, com o achado de que Campinas tem mais pedidos e fatura menos que Sorocaba. 30 flashcards e 24 perguntas acumuladas.
- Capítulo 03.07 (`JOIN` parte 1: `INNER`): a operação que dá nome ao modelo relacional, ensinada pela habilidade de **prever o número de linhas**. Contrasta o produto cartesiano (160) com a junção correta (20) e mostra a granularidade da tabela mais fina (31 itens em quatro tabelas). Registra o cartesiano disfarçado por `ON` sempre verdadeiro — que a verificação "nº de JOIN = nº de ON" não pega. O exercício AP3 reproduz de verdade a **soma dobrada** com duas tabelas filhas do mesmo pai: 2 itens × 2 pagamentos = 4 linhas, e os dois totais dobram de 53.970 para 107.940. 35 flashcards e 28 perguntas acumuladas.
- Capítulo 03.08 (`JOIN` parte 2: `LEFT`/`RIGHT`/`FULL`): fecha o bloco de junções com as perguntas respondidas **pela ausência**. O `NULL` fabricado pela junção — que não existe nos dados — é apresentado como sinal, e dele sai o padrão do anti-join (`WHERE direita.pk IS NULL`), que encontra o Rafael (nunca comprou) e o Mousepad (nunca vendido). O eixo é a armadilha de uma palavra: filtro sobre a direita no `WHERE` transforma `LEFT` em `INNER` em silêncio — 7 clientes contra 8, medido lado a lado. O desafio D1 integra as três lições do módulo numa consulta só: o `NULL` do `LEFT JOIN` (03.08), o comportamento das agregações (03.05) e a multiplicação de linhas (03.07), com `COUNT(DISTINCT p.id)` corrigindo um Carlos que aparecia com 6 pedidos em vez de 3. 40 flashcards e 32 perguntas acumuladas.
- Capítulo 03.09 (subconsultas): as três posições (`WHERE`, `FROM`, `SELECT`) e a distinção correlacionada × não correlacionada que decide o custo. Paga duas dívidas antigas — o `NOT EXISTS` prometido no 03.08 e "o produto mais caro de cada categoria", pendente desde o 03.04 e impossível com `LIMIT` ou `GROUP BY`. O eixo é a armadilha do `NOT IN` com `NULL`: **1** linha vira **0**, sem erro, e a consulta que funcionou por anos passa a devolver vazio no dia em que o primeiro nulo entra. O desafio D1 inclui a divisão relacional (`NOT EXISTS` dentro de `NOT EXISTS`) e expõe um empate real no laboratório — a Beatriz tem dois pedidos de R$ 899,00, e os dois aparecem no "maior pedido de cada cliente". 45 flashcards e 36 perguntas acumuladas.
- Capítulos 03.10 e 03.11 (CTEs com `WITH`; `INSERT`/`UPDATE`/`DELETE`): o problema das duas tabelas filhas, aberto no 03.07 e contornado no 03.09, resolvido na raiz — uma CTE por filha, cada uma reduzida a uma linha por pedido antes de qualquer junção. O 03.11 abre a escrita com o procedimento de cinco passos (rascunho, ensaio como `SELECT`, `BEGIN`, execução com o `WHERE` copiado, conferência das linhas afetadas) e demonstra as três redes de segurança: a chave estrangeira recusando um `DELETE`, o `ROLLBACK` desfazendo 13 linhas, e o rascunho descartável. Bug real corrigido no executor — `sql.py` confirmava cada comando e o `ROLLBACK` do aluno não desfazia nada (D-016); a correção virou conteúdo do §6.7. 55 flashcards e 44 perguntas de entrevista acumuladas no módulo. Decisões D-015 e D-016 registradas.
- Capítulo 03.12 (DDL e tipos de dados): a afinidade de tipos do SQLite demonstrada com `typeof()` — `'abacaxi'` gravado numa coluna `INTEGER` sem erro, `'42'` convertido para número na mesma coluna, `'3.7'` virando `real` e não `integer`, `BANANA` aceito como tipo e `VARCHAR(3)` guardando 26 caracteres. `STRICT` (3.37+) apresentado como a correção, com a nuance de que ele garante tipo e não faixa. As três decisões de toda base real justificadas por execução: dinheiro em centavos inteiros (`0.1 + 0.2 = 0.3` devolve falso), data em `TEXT` ISO (a ordem alfabética coincide com a cronológica; em `DD/MM/YYYY` não coincide) e booleano em `INTEGER` 0/1. Migração de tipo em quatro passos, com a armadilha do `CAST` sem `ROUND` — `CAST(19.99*100 AS INTEGER)` devolve 1998, perdendo um centavo por linha em silêncio. Novo banco descartável `dados/ddl.db` (D-017). 60 flashcards e 48 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.13 (constraints e integridade): as cinco restrições com a mensagem de erro real de cada violação, e a tese que organiza o capítulo — uma restrição recusa o **comprovadamente falso**, e `NULL` é desconhecido. Daí os dois buracos demonstrados por execução: três `NULL` convivem numa coluna `UNIQUE`, e um `NULL` atravessa `CHECK (status IN (...))` sem reclamação; a correção é `NOT NULL` junto, sempre. Documentado também o furo histórico do SQLite em que uma `PRIMARY KEY` de texto aceita `NULL` — fechado por `STRICT`. As três ações de `ON DELETE` executadas lado a lado, com a contradição `NOT NULL` + `SET NULL` que cria sem erro e quebra no primeiro `DELETE`, anos depois. E a limitação exata da conferência do 03.11: com `CASCADE`, apagar 1 cliente exibe `Linhas afetadas: 1` e remove 10 linhas — a conferência passa enquanto o histórico some. 65 flashcards e 52 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.14 (índices): o módulo troca de escala — novo laboratório `dados/indices.db` com 500.000 eventos e cardinalidades deliberadamente diferentes por coluna, mais um script de medição (`medir.py`) com conexão nova por medição e mediana de 7 repetições. O capítulo é construído sobre dois experimentos na **mesma tabela** com resultados opostos: índice em `cliente_id` (13 de 500.000 linhas) dá 763x; índice em `tipo` (100.271 linhas) dá ganho zero. E o caso que fecha o argumento, descoberto medindo: no filtro de março (12,5% da tabela), fazer `SCAN` virar `SEARCH` deixou a consulta **51% mais lenta** — o plano melhorou e o relógio piorou. Custo de escrita medido (0, 1 e 3 índices: 171 → 218 → 284 ms) e a conta de quando o índice deixa de compensar. Documentada também a armadilha de método que produziu uma conclusão errada na primeira tentativa: o SQLite guarda o plano em cache na conexão, e medir depois de criar um índice na mesma conexão mede o plano antigo (D-018). 70 flashcards e 56 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.15 (transações e ACID): a última caixa-preta do módulo, pendurada desde o 03.11. O capítulo é construído sobre uma tese demonstrada, não afirmada — as quatro garantias podem estar íntegras enquanto o resultado está errado. Cinco cenas com **duas conexões simultâneas** (`codigo/cap15/transacoes.py`, em Python porque concorrência exige dois clientes): o erro que **não** desfaz a transação e deixa um `COMMIT` gravar metade da operação (medido: 70000 em vez de 100000); o isolamento funcionando; `database is locked` entre dois escritores; o *lost update* perdendo R$ 100,00 sem mensagem nenhuma, mesmo no SQLite que implementa `SERIALIZABLE`; e as correções — operação no `SET`, `BEGIN IMMEDIATE` e bloqueio otimista com `rowcount`. Medições novas: o `UPDATE` com condição embutida vende exatamente 3 de 5 tentativas num estoque de 3, e o custo do agrupamento em 3.000 linhas (autocommit 10.609 ms · lotes de 100: 107 ms · transação única: 5,9 ms — **~1800x**). Novo banco `dados/tx.db` (D-019). 75 flashcards e 60 perguntas de entrevista acumuladas no módulo.
- **Módulo 03 completo**: capítulo 03.16 (modelagem e mini projeto) e pacote de fechamento. O 03.16 fecha o círculo aberto pela decisão D-014 — o schema da Aurora, consultado por quinze capítulos, agora é projetado: diagrama ER, normalização até a 3FN com a desnormalização deliberada justificada (`preco_unitario_centavos`, endereço de entrega, nota fiscal — o mesmo padrão três vezes), DDL completo com `STRICT`, `CHECK`, `UNIQUE` composta, ações de `ON DELETE` escolhidas uma a uma e índices só onde uma consulta os justifica. A migração é transacional e termina em conferência: schema diferente, faturamento idêntico ao centavo (831840). Catorze ataques ao schema, catorze recusas. E a descoberta que virou o AP3: trocar `cliente_id` entre dois pedidos preserva contagens **e** faturamento total — só um agregado por grupo detecta. Pacote de fechamento: `revisao/` (resumo, mapa mental com os três fios condutores do módulo, 15 questões, 80 flashcards), `Simulados/modulo-03` A e B, cheatsheet de SQL e 5 desafios de entrevista. Entrega Atlas: Aurora v2 — o negócio do módulo 01 em banco relacional, com as regras de negócio migradas do código para o schema. 80 flashcards e 64 perguntas de entrevista no módulo.
- Módulo 04 iniciado: visão do módulo (objetivos dos 23 capítulos, o fio condutor em três movimentos e três checkpoints internos) + capítulo 04.01 (`*args`, `**kwargs` e assinaturas flexíveis). O capítulo é construído em torno da armadilha do default mutável, demonstrada com `__defaults__` inspecionável antes e depois das chamadas — não há regra oculta, há um objeto num atributo. Documentada também a mesma armadilha em outra roupa (`datetime.now()` como default, avaliado na importação), a diferença entre empacotar (definição) e espalhar (chamada) nos três contextos em que a sintaxe aparece, e a construção de repasse `f(*args, **kwargs)` que sustenta os decoradores do 04.04. O gabarito registra a descoberta do A1.3: `print(f(1), f(2), f(3))` imprime `[1,2,3]` três vezes, porque o `print` avalia todos os argumentos antes de imprimir — você vê o estado final, não a evolução. E a tese que atravessa os exercícios: `*args`/`**kwargs` compram flexibilidade e **pagam com detecção de erro** — `registrar("falha", "ERRO")` manda o nível para os detalhes e loga como INFO, sem erro nenhum.
- Capítulos 04.02 e 04.03 (funções como valores; closures e fábricas): o bloco de funções avança pela pergunta "o que uma função pode ser além de um bloco de código". O 04.02 mede em vez de afirmar — um contador dentro do `key` mostra **1000 chamadas para 1000 elementos**, e não `n log n`, o que tem consequência prática direta (uma `key` que consulta banco faz `n` consultas). Documentados também: `map`/`filter` esgotando em silêncio na segunda leitura, o despacho por dicionário com o custo honesto (só casa igualdade exata, e a mensagem de erro passa a se gerar sozinha), e o critério `lambda` × `def` demonstrado no pipeline, onde `getattr(etapa, "__name__")` devolve `<lambda>` para toda etapa anônima. O 04.03 resolve o enigma que o 04.02 deixou aberto com a closure inspecionável — `__closure__[0].cell_contents` mostra o valor guardado —, e a prova definitiva do mecanismo é o A1.3: `n` reatribuído **depois** de a closure ser criada, e ela devolve 99. Registrada a regra que unifica `nonlocal`: ela é sobre a **variável**, não o objeto — `lista.append(1)` funciona sem declaração, `lista = [1]` não. E o AP3 chega à conclusão que prepara o 04.07: um dicionário com quatro funções já é um objeto montado à mão, sem `AttributeError` útil, sem docstring e sem `__repr__`. 15 flashcards e 12 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.04 (decoradores): o capítulo que junta os três anteriores e desmonta o `@` — nenhum mecanismo novo, só notação para função como valor (04.02) mais closure (04.03) mais repasse com `*args, **kwargs` (04.01). Demonstrados por execução: as duas formas (`@dec` e `funcao = dec(funcao)`) produzindo o mesmo resultado; o decorador rodando **na definição** (uma vez para três chamadas), que é o que permite o padrão de registro; os três níveis do decorador com argumentos; e a ordem de empilhamento, com os dois casos em que ela importa de verdade (`@cache` acima de `@autenticar` é falha de segurança; `@cronometrar` acima de `@cache` afunda a média). **Correção de uma crença comum, descoberta ao verificar:** `functools.wraps` **não** melhora o traceback — os frames são idênticos com e sem, porque o traceback lê `__code__.co_name` e `wraps` copia atributos da função, não o código compilado. O que ele de fato salva é a introspecção de assinatura (sem a qual FastAPI e pytest param de injetar) e o registro por `__name__` — com duas rotas decoradas sem `wraps`, ambas se chamam `envolvida` e a segunda **sobrescreve** a primeira, sem erro. O D1 reconstrói o `@app.get` e fecha com o critério do capítulo: envolva quando precisar agir a cada **chamada**; devolva a função inalterada quando o efeito acontece na **definição**. 20 flashcards e 16 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.05 (iteráveis e iteradores): responde a pergunta que o 04.02 deixou aberta — por que `map` esgota — com uma distinção só, demonstrada por `hasattr`: iterável tem `__iter__` e produz um percorredor novo a cada chamada; iterador tem `__iter__` **e** `__next__`, guarda posição, e `iter(it) is it`. Documentados: o `for` desmontado em `iter`/`next`/`StopIteration`; o arquivo aberto como iterador (a causa mais comum de "o relatório saiu pela metade"); e `range` como contraexemplo deliberado, para impedir a generalização de que preguiça implica esgotamento — são propriedades independentes. Três achados que viraram exercício: depois de um `break`, `list(it)` devolve `[3]` e não `[1,2,3]` nem `[]`, porque o iterador é uma posição; juntar iterável e iterador numa classe só produz **dois** sintomas, e o pior é que `for` aninhados devolvem 1 par em vez de n²; e remover itens durante o `for` sobre `[1,2,3,4,5]` **funciona por acaso** — só com `[2,4,6]` o defeito aparece (`[4]` em vez de `[]`), o que torna o bug perigoso, porque testado com a lista errada ele passa. O AP3 mede o custo real do `itertools.tee`: consumir só uma cópia de 200 mil elementos leva o pico a 9 MB, porque ele bufferiza tudo que a outra não leu — não é um `list()` preguiçoso. 25 flashcards e 20 perguntas de entrevista acumuladas no módulo.
- **Bloco de funções do módulo 04 completo**: capítulo 04.06 (geradores e `yield`), fechando a progressão 04.01–04.06 que responde "o que uma função pode ser além de um bloco de código" — valor, fábrica, envelope, protocolo e produtor pausável. O capítulo demonstra `yield` com `print` entre os `yield` (a única forma de ver onde a função pausa e retoma), refaz o Baralho do 04.05 em 4 linhas no lugar de 20 preservando as duas propriedades testáveis, e mede: um milhão de quadrados ocupa 40,3 MB em lista contra 0,0007 MB em gerador. **Correção de uma crença que eu mesmo tinha escrito:** "geradores são mais lentos" é falso como regra. Medido — somar uma lista já pronta é 4,4x mais rápido que somar um gerador sobre ela, mas num pipeline de 2 etapas o gerador foi 35% **mais rápido**, porque não paga a alocação das listas intermediárias; com 4 etapas, empatam. A §13 e o AP1 trazem os dois números e a conclusão de que há um cruzamento, não uma regra. Documentadas as três armadilhas: a validação que dispara no primeiro `next()` e escapa do `try/except` (com o padrão de duas funções que a corrige), o `return valor` que vira `StopIteration.value` descartado pelo `for`, e o gerador usado como coleção testada com `in` — que esgota e passa a responder `False`. O D1 pede a comparação honesta com o 04.05 e o caso em que duas classes vencem: quando o **estado da iteração** é parte da interface (retomada por checkpoint, relatório de progresso), porque o quadro congelado de um gerador é inacessível de fora. 30 flashcards e 24 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.07 (POO: classes e objetos): abre o bloco de POO exatamente onde o 04.03 parou — o dicionário de quatro funções do AP3 já era um objeto montado à mão. O argumento a favor de classes é apresentado como consequência observável, não como filosofia: errar o nome de um método dá `KeyError: 'incrementar'` na closure contra `AttributeError: 'Contador' object has no attribute 'incrementer'` na classe, e a segunda **nomeia o tipo**. Demonstrado que `self` não é mágico — `objeto.metodo()` e `Classe.metodo(objeto)` produzem o mesmo, `objeto.metodo.__self__` é a instância, e um método guardado em variável continua vinculado. O atributo de classe mutável é apresentado como o default mutável do 04.01 com outra roupa, com `Classe.tags is a.tags is b.tags` devolvendo `True` — e o par de exercícios A1.2/A1.3 isola a causa: `self.itens.append(v)` vaza, `self.itens = self.itens + [v]` não, que é a distinção mutar × reatribuir do 04.03 num terceiro contexto. O critério para escolher entre classe e dicionário é contável e não doutrinário: **três ou mais funções recebendo o mesmo dicionário e assumindo as mesmas chaves são métodos procurando uma classe**. E o AP3 obriga a reconhecer o que a closure faz melhor — encapsulamento real, já que `objeto.n = 999` sempre funciona numa classe. 35 flashcards e 28 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.08 (atributos, métodos e `self`): os três tipos de método apresentados pela pergunta que os decide — *do que este método precisa?* —, com uma quarta saída que costuma faltar nas explicações: se não precisa nem do objeto nem da classe **e** nenhuma subclasse vai sobrescrevê-lo, é **função de módulo**, não `staticmethod`. O teste que resolve a escolha entre `classmethod` e `staticmethod` é a **herança**, demonstrado com uma subclasse vazia: `ProdutoDigital.do_banco` devolve `ProdutoDigital` com `cls` e `Produto` com o nome fixo — sem erro, só um objeto do tipo errado circulando. O AP2 pede o cenário concreto do dano, e ele é bom: `entregar()` gerando etiqueta de envio físico para e-books, com o defeito aparecendo na operação e não no código. Descoberta ao verificar o A1.5: `self.contador += 1` para contar instâncias não "para em 1" — deixa o contador da classe em **zero**, porque a leitura vem da classe e a atribuição vai para a instância. E o D1 exige cache por classe, o que expõe a distinção entre `hasattr(cls, "_cache")` (acha o da mãe) e `"_cache" in cls.__dict__` (só o próprio) — fechando com os três problemas de cache de instâncias: vazamento de memória, aliasing invisível, e argumentos silenciosamente ignorados na segunda chamada. 40 flashcards e 32 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.09 (encapsulamento e properties): responde ao argumento que o 04.07/AP3 levantou — `objeto.n = 999` sempre funciona — apresentando quatro ferramentas e o que cada uma **de fato** impede. O mito do `__privado` é desmontado imprimindo o `__dict__`: `_Conta__secreto` está lá, à vista. E o propósito real do mangling é demonstrado com mãe e filha usando o mesmo nome de atributo — `_Base__estado` e `_Filha__estado` coexistindo, o que é proteção contra **colisão em herança**, não contra acesso. O argumento decisivo a favor de `@property` é a compatibilidade: numa classe usada em 40 lugares, acrescentar validação muda **zero** dessas linhas — e é por isso que Python dispensa getters preventivos. Medições novas: property custa ~45% na leitura (105,4 ms × 72,7 ms por 1M) e `__slots__` economiza 55% de memória (37,6 → 16,8 MB para 200 mil objetos). Duas armadilhas descobertas ao verificar: `__slots__ = ("a")` sem vírgula é uma **string**, não tupla, e funciona por acaso com um nome só; e uma subclasse **sem** `__slots__` recupera o `__dict__`, anulando proteção e economia — a garantia é local à classe, não à hierarquia. O D1 fecha com o furo honesto: `conta._saldo = -1000` passa, e nenhuma ferramenta do capítulo impede — encapsulamento em Python protege contra engano, não contra intenção, e a garantia real está na constraint do banco (03.13). 45 flashcards e 36 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.10 (herança): o capítulo corrige a definição mais repetida de `super()` — ele **não** é "chame a classe mãe", é "chame o próximo no MRO". A demonstração é um diamante `D(B, C)` em que o `super()` escrito dentro de `B` alcança `C`, uma classe que `B` sequer referencia: quem decide é o MRO da instância, não o código onde a chamada foi escrita. E o valor prático disso foi medido: com `super()`, `A.__init__` roda **uma** vez; chamando as mães pelo nome, roda **duas** — o problema do diamante em duas linhas de saída. Outras descobertas que viraram exercício: `super()` chamado **depois** do código da filha faz a mãe sobrescrever em silêncio (`self.x = "D"` seguido de `super()` devolve `'C'`); um `__repr__` herdado que cita o nome da classe literalmente mente sobre subclasses; e numa cadeia de `isinstance`, a **subclasse precisa vir antes da mãe**, ou o ramo dela nunca executa — medido com `Importado(Fisico)` devolvendo 1000 em vez de 5000, sem aviso. O D1 pede que se identifique o **ponto exato** em que a hierarquia deixa de compensar (a primeira classe que é uma **combinação**, não uma especialização) e fecha argumentando por que "prefira composição a herança" é um conselho útil e incompleto — ele não diz quando, ignora o custo da indireção, e desconsidera que frameworks inteiros são construídos sobre herança. 50 flashcards e 40 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.11 (composição vs. herança): substitui o conselho "prefira composição a herança" por um critério **contável** — a quantidade de eixos de variação independentes. Um eixo com poucos casos pede herança (três linhas); dois ou mais pedem composição, porque herança cresce como 2ⁿ e composição como n. O D1 mede: um relatório com quatro eixos de três opções exigiria **81 classes** por herança contra **12 objetos** por composição, e com um quinto eixo a diferença vai a 243 × 15. O argumento mais forte do capítulo, porém, não é o número de classes: é que herança múltipla **resolve conflitos em silêncio** — `DigitalImportado(Digital, Importado)` devolve o frete de `Digital` porque ela vem antes no MRO, e o valor de `Importado` nunca é consultado. Documentada a regra prática dos mixins que quase nunca aparece: **eles vêm antes da classe base**; com `class G(Base, Mixin)`, o MRO para em `Base` e o mixin fica na hierarquia sendo completamente ignorado, sem aviso. E um mixin com `__init__` quebra a cadeia — a hospedeira perde atributos. O AP2 usa `__init_subclass__` para registrar classes na definição, trazendo o padrão de registro do 04.04 para dentro de classes. O capítulo fecha com as três ressalvas contra composição — indireção, verbosidade na criação, e o conhecimento de montagem que sai da hierarquia — e com os três casos em que herança é a resposta certa, incluindo frameworks. 55 flashcards e 44 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.12 (métodos especiais): a família de dunder apresentada pela ideia que a unifica — Python define comportamento por **protocolo**, não por herança, e por isso qualquer objeto com `__len__` funciona em `len()` sem herdar de nada. O achado que organiza o capítulo é o efeito colateral de `__eq__`: definir igualdade por valor **apaga o `__hash__` herdado** e o objeto sai de `set` e de chaves de dicionário — porque o Python exige que objetos iguais tenham o mesmo hash e prefere remover a permitir a contradição. E o exercício A3.3 leva isso ao limite: um objeto cujo campo do hash é alterado depois de entrar num `set` **some** — `objeto in conjunto` devolve `False` enquanto `len(conjunto)` continua 1, sem erro nenhum. Documentados também: `__repr__` como reserva de `__str__` (inclusive em f-strings) e o fato de coleções sempre usarem `__repr__`; `__getitem__` dando quatro comportamentos de graça, incluindo iteração pelo protocolo antigo de sequência; o padrão que surpreende — objeto sem `__bool__` nem `__len__` é **sempre verdadeiro**, e um `if carrinho:` nunca é falso; e `NotImplemented` como a devolução correta para tipos desconhecidos, que produz `TypeError` claro em vez de `False` silencioso. O D1 constrói uma `Temperatura` completa e imutável, e pede honestidade em três casos de borda — sobretudo se `t1 - t2` devolve temperatura ou **diferença**, que são coisas fisicamente distintas. 60 flashcards e 48 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.13 (dataclasses): fecha o bloco de POO mostrando que as vinte linhas do 04.12 cabem em quatro — e onde a economia cobra o preço. O modelo mental é o que organiza tudo: `@dataclass` é um **gerador de código que roda uma vez, na definição**, monta o texto de `__init__`, `__repr__` e `__eq__`, executa e sai de cena; daí o `co_filename` ser `<string>`, o `inspect.getsource` falhar, e criar objetos custar exatamente o mesmo que numa classe escrita à mão (353,5 ms contra 355,7 ms por milhão — medido). A frase que governa o capítulo é "a anotação é a lista de campos", e dela saem os dois erros caros: `preco_centavos: int` **não verifica nada** (aceita string sem reclamar), e um atributo **sem** anotação não é campo — some do `__init__`, do `repr` e do `__eq__`, fazendo dois produtos com preços diferentes compararem como iguais, sem erro nem sintoma. Também documentados com execução: o `ValueError` que barra `itens: list = []` na definição da classe (a armadilha do 04.01, agora com barreira); `frozen=True` devolvendo o `__hash__` que o `__eq__` gerado bloqueou, e congelando só a superfície — a lista de dentro continua aceitando `append`, mas o `hash()` falha por causa dela, o que aqui é a proteção que resta; `order=True` comparando na **ordem de declaração**, de modo que reordenar campos por legibilidade muda a ordenação de todo o sistema em silêncio; e o `__post_init__`, que não pode atribuir numa classe congelada. Na §13, três medições que mudam decisão: `slots=True` corta 38% da memória (47,4 → 29,1 MB em 200 mil objetos), `asdict` é **32× mais lento** que montar o dicionário à mão porque faz cópia profunda, e definir mil dataclasses custa 417 ms contra 4,6 ms — no `import`, uma vez. O D1 usa o banco real do módulo 03 e termina em três perguntas de projeto, entre elas a descoberta de que `asdict` **ignora `@property`** e entregaria um JSON sem o total do pedido. 65 flashcards e 52 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.14 (type hints): abre a Caixa-preta que o 04.13 deixou (o que é `nome: str`) e a que o 04.11 prometeu (`Protocol`), e organiza o assunto em torno de uma frase — **a anotação é uma promessa que o Python não cobra e a ferramenta cobra**. Duas execuções que não se encontram: `python programa.py` ignora as anotações; `mypy programa.py` lê só elas, sem rodar nada. O par de arquivos `tipos.py` e `defeitos.py` transforma isso em demonstração: no segundo, o verificador aponta **seis erros de uma vez** e a execução mostra **um** e para — e dois deles (`dobrar("ab")` devolvendo `'abab'`, `sum(itens)/100` devolvendo float onde a assinatura promete int) nenhuma execução mostraria. O exercício A3 leva a comparação ao limite pedindo duas respostas independentes por trecho, e os **quatro pares acontecem**: o caso em que o mypy passa e o programa nem carrega (`-> Produto` sem aspas: `NameError`, porque o verificador resolve a referência adiante e o interpretador não) e o caso em que o mypy reclama e o programa está certo (`maior([1,2,3]) + 10`, porque ele lê a assinatura, não a chamada). A §6.8 documenta a armadilha que mais engana em projeto real: **"Success" mede cobertura de anotação, não correção** — `Any` desliga a verificação e função sem anotação nem é olhada, inclusive com `--check-untyped-defs`, já que parâmetro sem anotação **é** `Any`. Medições da §13: anotar **dobra** o custo de definir a função (233,5 contra 98,9 ms por 200 mil) e **não muda nada** na chamada (67,6 contra 69,1 ms por milhão); `from __future__ import annotations` derruba a definição para 89,8 ms, abaixo do código sem anotação. A §9 roda o verificador no próprio código do módulo 04 e encontra quatro apontamentos, **todos corretos e todos propositais** — o que vira a lição de que a ferramenta relata e você julga. E o AP3 fecha com uma limitação medida: `@runtime_checkable` + `isinstance` devolve `True` para uma classe cujo método tem o retorno errado, porque em execução só se confere que o método **existe**. 70 flashcards e 56 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.15 (Pydantic): paga a Caixa-preta 2 do 04.13 e fecha a lacuna que o 04.14 deixou explícita — o verificador lê **o seu código**, e o dado do cliente chega em execução. O modelo mental é a **alfândega**: um lugar só, na fronteira, que confere tudo, converte o que dá e recusa a remessa relatando **todos** os erros de uma passagem — a mesma estrutura dos seis apontamentos do mypy, agora sobre dados. A coerção é documentada por medição, não por descrição: `'8990'` vira 8990, `8990.0` passa e `8990.7` é recusado (`int_from_float`, porque nada pode se perder), `'8_990'` e `b'8990'` passam, e **`True` vira `1`** — um preço de um centavo, sem erro, pela mesma herança de `bool` ser subclasse de `int` que apareceu na ordenação do 04.13. Em português, `'yes'` passa como booleano e **`'sim'` não**; a data `'15/07/2026'` é recusada, e a recusa está certa. O achado mais caro do capítulo é a §6.6a: campo desconhecido é **descartado em silêncio**, de modo que `descconto_centavos=5000` — um `c` a mais — vira `desconto_centavos=0` e faz R$ 50,00 sumirem sem erro, aviso ou log; `extra="forbid"` é uma linha. Junto vêm os outros dois silêncios: atribuição depois da criação **não é validada** (a alfândega carimba na entrada), e `int | None` sem default é **obrigatório**, porque `| None` fala do tipo e não da obrigatoriedade. O `loc` do `ValidationError` ganha destaque próprio: `('itens', 1, 'quantidade')` é caminho **navegável por código**, e é ele que o FastAPI serializa no `422`. Medições da §13 que decidem a arquitetura: criar modelo custa 2,5× a dataclass, mas na fronteira o sinal inverte — `model_validate_json` é **1,9× mais rápido** que `json.loads` + construção e `model_dump_json` **3,7× mais rápido** que `json.dumps(asdict(...))`, enquanto ler atributo é 48% mais caro. Daí a regra medida: modelo na borda, dataclass no núcleo. A §9 paga a dívida do mini projeto do 04.13 — treze linhas de conversão e validação viram sete declarações com `BaseSettings` —, e o A3 traz dois defeitos que funcionam: um `field_validator` que lê outro campo e quebra conforme a **ordem de declaração**, e um validador **sem `return`** que guarda `None` num campo `str`, aprovado por uma biblioteca de validação. Primeiro código do manual sob a política do D-023: tipado e limpo em `mypy --strict`. 75 flashcards e 60 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.16 (ambientes virtuais e pip): abre a Caixa-preta do 04.14 e transforma os três `pip install` acumulados num laboratório executável. O `conflito.sh` cria dois ambientes com Pydantic 1 e 2 e roda **o mesmo arquivo** nos dois — `{"nome":"Mouse","preco_centavos":8990}` de um lado, `AttributeError: 'Produto' object has no attribute 'model_dump_json'` do outro, mesmo código e mesma máquina. A cena que fecha o argumento é a quinta: instalar a versão 1 sobre a 2 **rebaixa sem erro nenhum**, deixa `pydantic_core` órfão e produz um ambiente que nenhum `requirements.txt` descreve — e o `pip check` responde "No broken requirements found", porque ele confere declarações e não sabe qual versão o **seu** código esperava (a mesma forma do "Success" enganoso do 04.14/§6.8). O modelo mental é deliberadamente desmistificador: um ambiente é **uma pasta com um Python dentro**, e toda a diferença entre ter e não ter isolamento é o interpretador achar ou não um `pyvenv.cfg` de três linhas ao lado do próprio executável — por isso `activate` **só mexe no `PATH`** e `.venv/bin/python` chamado direto é equivalente. Daí sai o teste que o D1 e o mini projeto cobram: `sys.prefix != sys.base_prefix`, e não `$VIRTUAL_ENV`, que só existe se alguém digitou `activate` e faria um `verificar.sh` recusar editores, agendadores e integração contínua. A sexta cena isola a distinção mais fina do capítulo: na pasta renomeada, o `pip` quebra com `bad interpreter` e o `python` continua funcionando — descobrir o prefixo sobrevive a mudar de lugar, tê-lo gravado na primeira linha não. Sobre reprodutibilidade, a §6.6 é honesta em três níveis e mostra a folga que sobra: `pydantic==2.13.4` fixa o `pydantic-core` (o próprio Pydantic o fixou) e deixa `typing-extensions>=4.14.1` livre para mudar entre duas instalações do mesmo arquivo. Medições da §13: 4,2 s e 19 MB para um ambiente vazio (quase tudo é `pip` e `setuptools`; o interpretador é um link), 90 MB e 3.524 arquivos com Pydantic e mypy, e os 900 MB que dez projetos ocupam — o número que explica por que existe `uv`. Instruções para Windows incluídas. 80 flashcards e 64 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.17 (organização de projetos): abre a Caixa-preta do 04.16 (`pyproject.toml`) e organiza o assunto em torno de uma frase — **o seu projeto é encontrado por acidente ou por instalação, e a diferença aparece só na máquina dos outros**. O `vazamento.sh` demonstra o acidente de ponta a ponta: um projeto de layout plano com um `utilitarios.py` na raiz, importado pelo pacote, funciona na máquina de quem escreveu e falha com `ModuleNotFoundError` na de quem instala — porque o arquivo nunca fez parte do pacote, e o que foi empacotado é só `aurora/__init__.py` e `aurora/modelo.py`. O `imports.py` mede o mecanismo por trás: `sys.path[0]` é a pasta **do arquivo** em `python arquivo.py` e a pasta **atual** em `-m` e `-c`, o que faz `python loja/relatorio.py` dar `ModuleNotFoundError` **para o próprio pacote**; o import relativo quebra com "no known parent package" ao rodar o arquivo direto; e duas pastas de mesmo nome sem `__init__.py` **se fundem** num pacote só (PEP 420), com `__path__` trazendo as duas — a razão real de manter o arquivo hoje, junto com declarar a API pública. A §6.3 documenta o que quase nenhum texto sobre `src/` menciona: depois de `pip install -e .`, rodando **da pasta do projeto**, o módulo solto ainda é encontrado, porque a pasta atual continua no `sys.path` — e o `pytest` verde passa nos dois layouts com o defeito presente. O `src/` garante que **o pacote** venha da instalação; não garante que nada mais vaze, e o teste barato é sair da pasta e rodar de novo. A §13 traz um número que muda decisão de projeto: `import aurora` custa 21,3 ms e `import aurora.formato` custa **20,2 ms** — praticamente o mesmo, porque **importar qualquer submódulo executa o `__init__.py` primeiro**, e o nosso reexporta `Produto`, que arrasta `dataclasses` (18,7 ms); o mesmo arquivo fora do pacote custa 1,6 ms. O projeto de referência em `codigo/cap17/projeto-aurora/` é completo e verificado: layout `src/`, `pyproject.toml` com `requires-python`, grupo `dev`, `[project.scripts]` gerando o comando `aurora`, seis testes passando e `mypy --strict` limpo. 85 flashcards e 68 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.18 (datas, horas e fusos): o capítulo trata de uma classe de defeito que **não levanta exceção** — dos seis erros do A3, os seis funcionam —, e por isso é descoberta meses depois por alguém perguntando por que o número não fecha. O modelo mental separa duas coisas que compartilham o nome: um **instante** (ponto na história, igual para todos) e uma **leitura de relógio** (instante mais o lugar do mostrador), que no Python são o consciente e o ingênuo; daí a regra, **guarde instantes, mostre leituras**. A motivação é medida: `+ timedelta(days=1)` sobre 03/11/2018 em São Paulo avança **23 horas reais**, porque aquele domingo teve 23 horas — e o relatório do dia perde uma hora de vendas sem sinal nenhum. O recorte brasileiro dá ao capítulo um achado que nenhum texto internacional tem: 15 de janeiro ao meio-dia é `-02:00` em 2019 e `-03:00` em 2020, de modo que quem fixou `-03:00` acerta os dados de hoje e **erra uma hora em todo dado de verão anterior a 2020** — e a base histórica de qualquer empresa brasileira com mais de sete anos tem essa fronteira no meio. As transições rendem os dois casos que ninguém testa: a hora **repetida**, em que os dois `datetime` têm o mesmo hash, comparam-se como **iguais** e um deles **desaparece de um `set`** estando a 3600 s de distância (o `fold` os distingue e não entra na comparação, por decisão da PEP 495); e a hora que **nunca existiu**, que o Python aceita sem reclamar escolhendo um instante. Documentados também: `utcnow()` devolvendo hora de UTC sem marcá-la como UTC (obsoleto no 3.12); `fromisoformat` **recusando o `"Z"`** até o 3.10, embora seja o que toda API devolve; a ordenação por texto saindo trocada quando dois registros do mesmo instante têm offsets diferentes; aniversário como `datetime` virando 14 de julho para quem está a oeste; e `perf_counter` contra `datetime.now()` para duração — a diferença não é de precisão (50,2 × 50,1 ms) e sim de garantia, já que `get_clock_info("time").monotonic` é `False`. Na borda, o Pydantic aceita ingênuo por padrão, aceita o `"Z"` que o `fromisoformat` recusa, e converte **um inteiro** em data; `AwareDatetime` resolve o primeiro. O mini projeto fecha com uma técnica que vale sozinha: detectar hora inexistente por **ida e volta** a UTC, e a tabela de duas colunas que separa a inexistente da ambígua. 90 flashcards e 72 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.19 (logging): fecha o bloco de profissionalização e substitui o `print` por registro utilizável. A §3 abre com cinco perguntas que um `print` não responde — que horas foi, qual pedido, de qual módulo, onde está o rastro, dá para ver só os erros — e a §9 volta a elas mostrando cada uma respondida por uma linha de log em JSON. O modelo mental separa **três decisões independentes** que costumam ser confundidas — quem escreve (`logger` por módulo), quem filtra (o nível) e para onde vai (o handler) —, e a frase que organiza tudo é que **quem escreve não decide para onde vai**. As armadilhas silenciosas são o coração do capítulo, e uma delas foi descoberta na verificação: além do nível padrão ser `WARNING`, há um **segundo filtro** que quase nenhum texto menciona — depois de `setLevel(INFO)`, a mensagem passa pelo logger (`isEnabledFor` devolve `True`) e **morre no handler de último recurso**, cujo nível é fixo em `WARNING` e não se ajusta pelo logger. Documentadas também: `basicConfig` chamado uma segunda vez **não faz nada e não avisa**; e o formatador com campo obrigatório ausente que **descarta a mensagem inteira**, jogando um despejo em `stderr` no lugar exato do registro que serviria para investigar — a pior falha possível num sistema de log. A §6.4 mede a formatação preguiçosa com números que mudam a prática: com `DEBUG` desligado, `log.debug("x %s", caro)` custa 54,8 ms por 200 mil e `log.debug(f"x {caro}")` custa 139,4 ms — e o custo da primeira **não muda com o valor**, porque ela nem formata. O capítulo também registra a distinção que quase nunca é dita: `print` escreve na **saída** do programa e log no **diário**, de modo que um script redirecionado para CSV recebe as mensagens de depuração dentro do arquivo. O `demonstracao.py` roda cada cena num **processo separado**, e o motivo é conteúdo: a configuração de log é global ao processo, e uma cena que chame `basicConfig` estraga todas as seguintes. Primeiro capítulo com `registro.py` completo — `configurar()` com `force=True`, `FormatadorJSON` com carimbo em UTC (04.18) e os campos de `extra=` descobertos **programaticamente** a partir de um `LogRecord` vazio, em vez de uma lista escrita à mão que quebraria na próxima versão do Python. 95 flashcards e 76 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.20 (context managers): paga a Caixa-preta 2 do 04.12 e apresenta o `with` pela frase que o resume — **a garantia mora no recurso, não em quem o usa**. O `__exit__` **recebe a exceção** (tipo, valor e rastro, ou três `None`), e é isso que permite a uma transação decidir sozinha entre `COMMIT` e `ROLLBACK`; devolver um valor **verdadeiro** dali **suprime** a exceção, sem log e sem rastro, numa linha que mora na classe do recurso e que quem escreveu o `with` nunca vê. A §6.4 mede o `@contextmanager` sem `try/finally`: a exceção é relançada **no ponto do `yield`** e mata o resto da função, de modo que a limpeza não roda — e o A3 leva isso ao caso caro, um bloqueio cujo `release()` é pulado e deixa o próximo processo esperando para sempre, **sem erro nenhum**. Documentado também o gerador de uso único, que falha na segunda vez com `AttributeError: args`, uma mensagem que não explica nada. O achado que conecta com o módulo 03 é a §6.5: **`with conexao:` no `sqlite3` não fecha a conexão** — ele controla a transação, e num laço isso esgota o limite do banco; a forma correta são dois `with` aninhados, porque são duas garantias diferentes. O A1 mapeia oito formas de sair de um bloco e encontra as duas em que o `__exit__` **não** roda: `os._exit()`, que encerra o processo no ato, e a exceção no próprio `__enter__` — daí a regra de que o que ele adquire antes de falhar é responsabilidade dele. Medição da §13: entrar e sair um milhão de vezes custa **240,1 ms** numa classe contra **1511,1 ms** num `@contextmanager`, cerca de 6× — invisível para um arquivo, visível num laço quente. O D1 constrói o gerenciador `operacao(nome, **contexto)` que resolve de uma vez as quatro lacunas que o AP3 do 04.19 revelou, e a segunda pergunta traz um achado verificado: aninhar duas operações e falhar na interna produz **duas** mensagens de erro com o mesmo rastro, quando deveria produzir uma — o erro do 04.19/§6.2 cometido pelo próprio gerenciador, e em toda camada de aninhamento. 100 flashcards e 80 perguntas de entrevista acumuladas no módulo.
