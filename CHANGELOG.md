# Changelog — Especificação do Manual Mestre

Formato: [Keep a Changelog](https://keepachangelog.com/) simplificado. Uma entrada por alteração da especificação.

## [3.0.0] — 2026-07-30

### Adicionado
- Especificação de arquitetura completa (`manualMestre_v3.0.md`), substituindo integralmente a v2.0.
- Início da geração: estrutura do repositório, arquivos de raiz, `00-visao-do-modulo.md` do módulo 00 e capítulo 00.01.
- Módulo 00 completo (capítulos 00.02–00.05, exercícios e gabaritos, guias de ambiente, pacote de revisão, simulados CP2 variantes A/B, cheatsheet de método). Decisões D-004 a D-006 registradas.
- Módulo 01 iniciado: visão do módulo (objetivos dos 25 capítulos) + capítulos 01.01–01.04 com código executável, exercícios, gabaritos, flashcards e banco de entrevistas.
- Capítulos 01.05–01.08 (strings 1 e 2, entrada/saída, booleanos): código executável testado, exercícios e gabaritos, 40 flashcards e 25 perguntas de entrevista acumuladas no módulo.
- Capítulos 01.09–01.12 (condicionais, while, for/range, listas 1): promessas didáticas pagas (troco e tabela refatorados), 60 flashcards e 40 perguntas acumuladas.
- Capítulos 01.13–01.16 (aliasing/cópias, tuplas, dicionários, conjuntos): quarteto de estruturas completo; a dor original da Aurora ("quanto vendemos por cidade") respondida em 01.15. 80 flashcards e 55 perguntas acumuladas.
- Capítulos 01.17–01.20 (compreensões, funções 1 e 2, módulos/imports): a caixa-preta mais antiga da trilha (`if __name__ == "__main__"`, prometida em 01.02) aberta; biblioteca + dois programas com zero duplicação. 100 flashcards e 70 perguntas acumuladas.
- **Módulo 01 completo**: capítulos 01.21–01.25 (exceções, arquivos/CSV, JSON, depuração, PEP 8 + mini projeto) e pacote de fechamento — `revisao/` (resumo, mapa mental, 15 questões), `Simulados/modulo-01` A e B, cheatsheet de Python e 5 desafios de entrevista. Entrega Atlas: Relatório de Vendas Aurora v0. 125 flashcards no módulo.
- Módulo 02 iniciado: visão do módulo (objetivos dos 12 capítulos) + capítulos 02.01–02.04 (terminal, navegação/manipulação, inspeção, pipes/grep/find) com scripts de caderno executáveis, exercícios, gabaritos, 20 flashcards e 13 perguntas de entrevista. Decisão D-009 registrada (bash/Git Bash como shell de referência).
- Capítulos 02.05–02.08 (permissões/processos, variáveis de ambiente e PATH, scripts de shell, Git como modelo mental): a caixa-preta do PATH aberta desde o 00.03 e a da pasta `.git` vista no 02.01 fechadas; padrão de configuração por ambiente e `.env` fora do Git antecipado para o módulo 06. Scripts executados e conferidos no sandbox, incluindo o `verificar_manual.sh`, que audita o próprio repositório e devolve código de saída. 40 flashcards e 29 perguntas de entrevista acumuladas no módulo. Decisão D-010 registrada.
- **Módulo 02 completo**: capítulos 02.09–02.12 (fluxo essencial do Git com `.gitignore`, branches e merge, remotos/GitHub, desfazendo) e pacote de fechamento — `revisao/` (resumo, mapa mental, 15 questões), `Simulados/modulo-02` A e B, cheatsheet de Git/Linux e 4 desafios de entrevista. Entrega Atlas: repositório publicado no GitHub com fluxo de branches e histórico legível. 60 flashcards e 45 perguntas de entrevista no módulo. Decisões D-011 e D-012 registradas.
- Módulo 03 iniciado: visão do módulo (objetivos dos 16 capítulos, fio condutor em três movimentos) + capítulo 03.01 (por que bancos relacionais existem). Laboratório SQLite criado — `codigo/cap01/criar_laboratorio.py` gera o banco da Aurora com 4 tabelas e 71 linhas, e `codigo/sql.py` executa consultas de arquivo, argumento ou modo interativo. Casos de ensino embutidos nos dados (cliente sem compras, e-mail NULL, produto nunca vendido, pedido cancelado). Decisões D-013 e D-014 registradas.
- Capítulo 03.02 (tabelas, linhas e chaves): chave primária artificial, chave estrangeira e integridade referencial demonstradas com as **quatro recusas reais** do banco (FK inexistente, PK duplicada, NOT NULL, DELETE com dependentes), cada mensagem de erro verificada. Leitura de schema com `sqlite_master` e `pragma_table_info` como habilidade prática. 10 flashcards e 8 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.03 (`SELECT` e `WHERE`): operadores de filtro, `LIKE` com nota de dialeto (o SQLite ignora maiúsculas em ASCII e nunca ignora acento), precedência de `AND`/`OR` demonstrada com resultados divergentes (3 linhas × 1 linha), e a **lógica de três valores** do `NULL` — com as duas armadilhas silenciosas reproduzidas: `= NULL` devolvendo zero linhas e a negação que exclui os nulos (3 + 4 ≠ 8). 15 flashcards e 12 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.04 (ordenação, `LIMIT` e `DISTINCT`): `ORDER BY` com desempate, posição do `NULL` na ordenação (com a forma portável), `AS` e a ordem de execução que explica por que o apelido vale no `ORDER BY` e não no `WHERE`. O eixo do capítulo é o **bug de paginação sem ordenação total**, tratado até a paginação por cursor — com o reconhecimento honesto de que a versão frágil não quebra no laboratório estático, e é por isso que ele sobrevive em produção. 20 flashcards e 16 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.05 (funções de agregação): as cinco funções e a **regra única** — todas ignoram `NULL`, exceto `COUNT(*)`. Dela derivam as três consequências demonstradas com números reais: contagens que discordam (8/7/7/3 na mesma tabela), a média que muda de denominador (7,43 × 6,50) e o `SUM` de conjunto vazio devolvendo `NULL` em vez de zero. Antecipa a multiplicação de linhas da junção (28 × 17) e a prova dos nove feita **em centavos inteiros**, porque em reais o ponto flutuante não fecha. 25 flashcards e 20 perguntas acumuladas no módulo.
- Capítulo 03.06 (`GROUP BY` e `HAVING`): fecha o arco mais antigo da trilha — o `chave → acumulador` do 01.15 e o `sort | uniq -c` do 02.04 viram uma linha declarativa. A distinção `WHERE` (linhas, antes) × `HAVING` (grupos, depois) é demonstrada com a mesma tabela e o mesmo limiar produzindo resultados diferentes (audio com 3 e com 4). Registra que o `NULL` **forma seu próprio grupo** (invertendo a intuição do `WHERE`) e o terceiro caso de permissividade perigosa do SQLite: coluna fora do `GROUP BY` devolve valor arbitrário sem erro. Entrega a dor da Aurora respondida — faturamento por cidade, com o achado de que Campinas tem mais pedidos e fatura menos que Sorocaba. 30 flashcards e 24 perguntas acumuladas.
- Capítulo 03.07 (`JOIN` parte 1: `INNER`): a operação que dá nome ao modelo relacional, ensinada pela habilidade de **prever o número de linhas**. Contrasta o produto cartesiano (160) com a junção correta (20) e mostra a granularidade da tabela mais fina (31 itens em quatro tabelas). Registra o cartesiano disfarçado por `ON` sempre verdadeiro — que a verificação "nº de JOIN = nº de ON" não pega. O exercício AP3 reproduz de verdade a **soma dobrada** com duas tabelas filhas do mesmo pai: 2 itens × 2 pagamentos = 4 linhas, e os dois totais dobram de 53.970 para 107.940. 35 flashcards e 28 perguntas acumuladas.
- Capítulo 03.08 (`JOIN` parte 2: `LEFT`/`RIGHT`/`FULL`): fecha o bloco de junções com as perguntas respondidas **pela ausência**. O `NULL` fabricado pela junção — que não existe nos dados — é apresentado como sinal, e dele sai o padrão do anti-join (`WHERE direita.pk IS NULL`), que encontra o Rafael (nunca comprou) e o Mousepad (nunca vendido). O eixo é a armadilha de uma palavra: filtro sobre a direita no `WHERE` transforma `LEFT` em `INNER` em silêncio — 7 clientes contra 8, medido lado a lado. O desafio D1 integra as três lições do módulo numa consulta só: o `NULL` do `LEFT JOIN` (03.08), o comportamento das agregações (03.05) e a multiplicação de linhas (03.07), com `COUNT(DISTINCT p.id)` corrigindo um Carlos que aparecia com 6 pedidos em vez de 3. 40 flashcards e 32 perguntas acumuladas.
- Capítulo 03.09 (subconsultas): as três posições (`WHERE`, `FROM`, `SELECT`) e a distinção correlacionada × não correlacionada que decide o custo. Paga duas dívidas antigas — o `NOT EXISTS` prometido no 03.08 e "o produto mais caro de cada categoria", pendente desde o 03.04 e impossível com `LIMIT` ou `GROUP BY`. O eixo é a armadilha do `NOT IN` com `NULL`: **1** linha vira **0**, sem erro, e a consulta que funcionou por anos passa a devolver vazio no dia em que o primeiro nulo entra. O desafio D1 inclui a divisão relacional (`NOT EXISTS` dentro de `NOT EXISTS`) e expõe um empate real no laboratório — a Beatriz tem dois pedidos de R$ 899,00, e os dois aparecem no "maior pedido de cada cliente". 45 flashcards e 36 perguntas acumuladas.
- Capítulos 03.10 e 03.11 (CTEs com `WITH`; `INSERT`/`UPDATE`/`DELETE`): o problema das duas tabelas filhas, aberto no 03.07 e contornado no 03.09, resolvido na raiz — uma CTE por filha, cada uma reduzida a uma linha por pedido antes de qualquer junção. O 03.11 abre a escrita com o procedimento de cinco passos (rascunho, ensaio como `SELECT`, `BEGIN`, execução com o `WHERE` copiado, conferência das linhas afetadas) e demonstra as três redes de segurança: a chave estrangeira recusando um `DELETE`, o `ROLLBACK` desfazendo 13 linhas, e o rascunho descartável. Bug real corrigido no executor — `sql.py` confirmava cada comando e o `ROLLBACK` do aluno não desfazia nada (D-016); a correção virou conteúdo do §6.7. 55 flashcards e 44 perguntas de entrevista acumuladas no módulo. Decisões D-015 e D-016 registradas.
- Capítulo 03.12 (DDL e tipos de dados): a afinidade de tipos do SQLite demonstrada com `typeof()` — `'abacaxi'` gravado numa coluna `INTEGER` sem erro, `'42'` convertido para número na mesma coluna, `'3.7'` virando `real` e não `integer`, `BANANA` aceito como tipo e `VARCHAR(3)` guardando 26 caracteres. `STRICT` (3.37+) apresentado como a correção, com a nuance de que ele garante tipo e não faixa. As três decisões de toda base real justificadas por execução: dinheiro em centavos inteiros (`0.1 + 0.2 = 0.3` devolve falso), data em `TEXT` ISO (a ordem alfabética coincide com a cronológica; em `DD/MM/YYYY` não coincide) e booleano em `INTEGER` 0/1. Migração de tipo em quatro passos, com a armadilha do `CAST` sem `ROUND` — `CAST(19.99*100 AS INTEGER)` devolve 1998, perdendo um centavo por linha em silêncio. Novo banco descartável `dados/ddl.db` (D-017). 60 flashcards e 48 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.13 (constraints e integridade): as cinco restrições com a mensagem de erro real de cada violação, e a tese que organiza o capítulo — uma restrição recusa o **comprovadamente falso**, e `NULL` é desconhecido. Daí os dois buracos demonstrados por execução: três `NULL` convivem numa coluna `UNIQUE`, e um `NULL` atravessa `CHECK (status IN (...))` sem reclamação; a correção é `NOT NULL` junto, sempre. Documentado também o furo histórico do SQLite em que uma `PRIMARY KEY` de texto aceita `NULL` — fechado por `STRICT`. As três ações de `ON DELETE` executadas lado a lado, com a contradição `NOT NULL` + `SET NULL` que cria sem erro e quebra no primeiro `DELETE`, anos depois. E a limitação exata da conferência do 03.11: com `CASCADE`, apagar 1 cliente exibe `Linhas afetadas: 1` e remove 10 linhas — a conferência passa enquanto o histórico some. 65 flashcards e 52 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.14 (índices): o módulo troca de escala — novo laboratório `dados/indices.db` com 500.000 eventos e cardinalidades deliberadamente diferentes por coluna, mais um script de medição (`medir.py`) com conexão nova por medição e mediana de 7 repetições. O capítulo é construído sobre dois experimentos na **mesma tabela** com resultados opostos: índice em `cliente_id` (13 de 500.000 linhas) dá 763x; índice em `tipo` (100.271 linhas) dá ganho zero. E o caso que fecha o argumento, descoberto medindo: no filtro de março (12,5% da tabela), fazer `SCAN` virar `SEARCH` deixou a consulta **51% mais lenta** — o plano melhorou e o relógio piorou. Custo de escrita medido (0, 1 e 3 índices: 171 → 218 → 284 ms) e a conta de quando o índice deixa de compensar. Documentada também a armadilha de método que produziu uma conclusão errada na primeira tentativa: o SQLite guarda o plano em cache na conexão, e medir depois de criar um índice na mesma conexão mede o plano antigo (D-018). 70 flashcards e 56 perguntas de entrevista acumuladas no módulo.
- Capítulo 03.15 (transações e ACID): a última caixa-preta do módulo, pendurada desde o 03.11. O capítulo é construído sobre uma tese demonstrada, não afirmada — as quatro garantias podem estar íntegras enquanto o resultado está errado. Cinco cenas com **duas conexões simultâneas** (`codigo/cap15/transacoes.py`, em Python porque concorrência exige dois clientes): o erro que **não** desfaz a transação e deixa um `COMMIT` gravar metade da operação (medido: 70000 em vez de 100000); o isolamento funcionando; `database is locked` entre dois escritores; o *lost update* perdendo R$ 100,00 sem mensagem nenhuma, mesmo no SQLite que implementa `SERIALIZABLE`; e as correções — operação no `SET`, `BEGIN IMMEDIATE` e bloqueio otimista com `rowcount`. Medições novas: o `UPDATE` com condição embutida vende exatamente 3 de 5 tentativas num estoque de 3, e o custo do agrupamento em 3.000 linhas (autocommit 10.609 ms · lotes de 100: 107 ms · transação única: 5,9 ms — **~1800x**). Novo banco `dados/tx.db` (D-019). 75 flashcards e 60 perguntas de entrevista acumuladas no módulo.
- **Módulo 03 completo**: capítulo 03.16 (modelagem e mini projeto) e pacote de fechamento. O 03.16 fecha o círculo aberto pela decisão D-014 — o schema da Aurora, consultado por quinze capítulos, agora é projetado: diagrama ER, normalização até a 3FN com a desnormalização deliberada justificada (`preco_unitario_centavos`, endereço de entrega, nota fiscal — o mesmo padrão três vezes), DDL completo com `STRICT`, `CHECK`, `UNIQUE` composta, ações de `ON DELETE` escolhidas uma a uma e índices só onde uma consulta os justifica. A migração é transacional e termina em conferência: schema diferente, faturamento idêntico ao centavo (831840). Catorze ataques ao schema, catorze recusas. E a descoberta que virou o AP3: trocar `cliente_id` entre dois pedidos preserva contagens **e** faturamento total — só um agregado por grupo detecta. Pacote de fechamento: `revisao/` (resumo, mapa mental com os três fios condutores do módulo, 15 questões, 80 flashcards), `Simulados/modulo-03` A e B, cheatsheet de SQL e 5 desafios de entrevista. Entrega Atlas: Aurora v2 — o negócio do módulo 01 em banco relacional, com as regras de negócio migradas do código para o schema. 80 flashcards e 64 perguntas de entrevista no módulo.
- Módulo 04 iniciado: visão do módulo (objetivos dos 23 capítulos, o fio condutor em três movimentos e três checkpoints internos) + capítulo 04.01 (`*args`, `**kwargs` e assinaturas flexíveis). O capítulo é construído em torno da armadilha do default mutável, demonstrada com `__defaults__` inspecionável antes e depois das chamadas — não há regra oculta, há um objeto num atributo. Documentada também a mesma armadilha em outra roupa (`datetime.now()` como default, avaliado na importação), a diferença entre empacotar (definição) e espalhar (chamada) nos três contextos em que a sintaxe aparece, e a construção de repasse `f(*args, **kwargs)` que sustenta os decoradores do 04.04. O gabarito registra a descoberta do A1.3: `print(f(1), f(2), f(3))` imprime `[1,2,3]` três vezes, porque o `print` avalia todos os argumentos antes de imprimir — você vê o estado final, não a evolução. E a tese que atravessa os exercícios: `*args`/`**kwargs` compram flexibilidade e **pagam com detecção de erro** — `registrar("falha", "ERRO")` manda o nível para os detalhes e loga como INFO, sem erro nenhum.
- Capítulos 04.02 e 04.03 (funções como valores; closures e fábricas): o bloco de funções avança pela pergunta "o que uma função pode ser além de um bloco de código". O 04.02 mede em vez de afirmar — um contador dentro do `key` mostra **1000 chamadas para 1000 elementos**, e não `n log n`, o que tem consequência prática direta (uma `key` que consulta banco faz `n` consultas). Documentados também: `map`/`filter` esgotando em silêncio na segunda leitura, o despacho por dicionário com o custo honesto (só casa igualdade exata, e a mensagem de erro passa a se gerar sozinha), e o critério `lambda` × `def` demonstrado no pipeline, onde `getattr(etapa, "__name__")` devolve `<lambda>` para toda etapa anônima. O 04.03 resolve o enigma que o 04.02 deixou aberto com a closure inspecionável — `__closure__[0].cell_contents` mostra o valor guardado —, e a prova definitiva do mecanismo é o A1.3: `n` reatribuído **depois** de a closure ser criada, e ela devolve 99. Registrada a regra que unifica `nonlocal`: ela é sobre a **variável**, não o objeto — `lista.append(1)` funciona sem declaração, `lista = [1]` não. E o AP3 chega à conclusão que prepara o 04.07: um dicionário com quatro funções já é um objeto montado à mão, sem `AttributeError` útil, sem docstring e sem `__repr__`. 15 flashcards e 12 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.04 (decoradores): o capítulo que junta os três anteriores e desmonta o `@` — nenhum mecanismo novo, só notação para função como valor (04.02) mais closure (04.03) mais repasse com `*args, **kwargs` (04.01). Demonstrados por execução: as duas formas (`@dec` e `funcao = dec(funcao)`) produzindo o mesmo resultado; o decorador rodando **na definição** (uma vez para três chamadas), que é o que permite o padrão de registro; os três níveis do decorador com argumentos; e a ordem de empilhamento, com os dois casos em que ela importa de verdade (`@cache` acima de `@autenticar` é falha de segurança; `@cronometrar` acima de `@cache` afunda a média). **Correção de uma crença comum, descoberta ao verificar:** `functools.wraps` **não** melhora o traceback — os frames são idênticos com e sem, porque o traceback lê `__code__.co_name` e `wraps` copia atributos da função, não o código compilado. O que ele de fato salva é a introspecção de assinatura (sem a qual FastAPI e pytest param de injetar) e o registro por `__name__` — com duas rotas decoradas sem `wraps`, ambas se chamam `envolvida` e a segunda **sobrescreve** a primeira, sem erro. O D1 reconstrói o `@app.get` e fecha com o critério do capítulo: envolva quando precisar agir a cada **chamada**; devolva a função inalterada quando o efeito acontece na **definição**. 20 flashcards e 16 perguntas de entrevista acumuladas no módulo.
- Capítulo 04.05 (iteráveis e iteradores): responde a pergunta que o 04.02 deixou aberta — por que `map` esgota — com uma distinção só, demonstrada por `hasattr`: iterável tem `__iter__` e produz um percorredor novo a cada chamada; iterador tem `__iter__` **e** `__next__`, guarda posição, e `iter(it) is it`. Documentados: o `for` desmontado em `iter`/`next`/`StopIteration`; o arquivo aberto como iterador (a causa mais comum de "o relatório saiu pela metade"); e `range` como contraexemplo deliberado, para impedir a generalização de que preguiça implica esgotamento — são propriedades independentes. Três achados que viraram exercício: depois de um `break`, `list(it)` devolve `[3]` e não `[1,2,3]` nem `[]`, porque o iterador é uma posição; juntar iterável e iterador numa classe só produz **dois** sintomas, e o pior é que `for` aninhados devolvem 1 par em vez de n²; e remover itens durante o `for` sobre `[1,2,3,4,5]` **funciona por acaso** — só com `[2,4,6]` o defeito aparece (`[4]` em vez de `[]`), o que torna o bug perigoso, porque testado com a lista errada ele passa. O AP3 mede o custo real do `itertools.tee`: consumir só uma cópia de 200 mil elementos leva o pico a 9 MB, porque ele bufferiza tudo que a outra não leu — não é um `list()` preguiçoso. 25 flashcards e 20 perguntas de entrevista acumuladas no módulo.
- **Bloco de funções do módulo 04 completo**: capítulo 04.06 (geradores e `yield`), fechando a progressão 04.01–04.06 que responde "o que uma função pode ser além de um bloco de código" — valor, fábrica, envelope, protocolo e produtor pausável. O capítulo demonstra `yield` com `print` entre os `yield` (a única forma de ver onde a função pausa e retoma), refaz o Baralho do 04.05 em 4 linhas no lugar de 20 preservando as duas propriedades testáveis, e mede: um milhão de quadrados ocupa 40,3 MB em lista contra 0,0007 MB em gerador. **Correção de uma crença que eu mesmo tinha escrito:** "geradores são mais lentos" é falso como regra. Medido — somar uma lista já pronta é 4,4x mais rápido que somar um gerador sobre ela, mas num pipeline de 2 etapas o gerador foi 35% **mais rápido**, porque não paga a alocação das listas intermediárias; com 4 etapas, empatam. A §13 e o AP1 trazem os dois números e a conclusão de que há um cruzamento, não uma regra. Documentadas as três armadilhas: a validação que dispara no primeiro `next()` e escapa do `try/except` (com o padrão de duas funções que a corrige), o `return valor` que vira `StopIteration.value` descartado pelo `for`, e o gerador usado como coleção testada com `in` — que esgota e passa a responder `False`. O D1 pede a comparação honesta com o 04.05 e o caso em que duas classes vencem: quando o **estado da iteração** é parte da interface (retomada por checkpoint, relatório de progresso), porque o quadro congelado de um gerador é inacessível de fora. 30 flashcards e 24 perguntas de entrevista acumuladas no módulo.
